<p id="notice"><%= notice %></p>

<p>
  <strong>Nazwa:</strong>
  <%= @idea.name %>
</p>

<p>
  <strong>Dlaczego:</strong>
  <%= @idea.description %>
</p>

<p>
  <strong>Obraz (nuty):</strong>
  <%= image_tag(@idea.picture_url, :width => 600) if @idea.picture.present? %>
</p>

<%= link_to 'Edit', edit_idea_path(@idea) %> |
<%= link_to 'Back', ideas_path %>
